package com.analyzer.analyzer.price_variation;

import org.springframework.data.jpa.repository.JpaRepository;

public interface PriceVariationRepository extends JpaRepository<PriceVariation, Integer> {
}
